version: "3.9"

services:
  sentiment-ai:
    build: .
    container_name: sentiment-intelligence
    ports:
      - "8501:8501"
    env_file:
      - .env.example
    volumes:
      - model-cache:/root/.cache/huggingface
    restart: unless-stopped

volumes:
  model-cache:
